#  Text Analyser

## 1. Reading the path name from the command line as argument

We will use argparse to take argument with the flag --path for which we will be using the add_argument method and then by parsing the args using parser_args we can get args.path that will contain the path. 